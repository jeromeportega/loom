{
  "name": "loom-ai",
  "version": "5.9.0",
  "description": "Loom — a self-learning, autonomous agentic engineering system. Write a brief, approve the plan, agents deliver the epic.",
  "keywords": ["ai", "agents", "autonomous", "claude", "code-generation", "cli", "mcp"],
  "license": "UNLICENSED",
  "homepage": "https://github.com/jeromeportega/loom",
  "repository": { "type": "git", "url": "git+https://github.com/jeromeportega/loom.git" },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "commonjs",
  "bin": {
    "loom": "dist/index.js",
    "loom-bench": "dist/loom-bench.js"
  },
  "files": ["dist"],
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "rm -rf dist && tsc && chmod +x dist/index.js $(test -f dist/loom-bench.js && echo dist/loom-bench.js)",
    "test": "node --test $(find dist -path '*__tests__*' -name '*.test.js')",
    "test:exec-bit": "npm run build && node --test dist/__tests__/execBit.test.js",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@loom-ai/core": "^5.9.0",
    "@loom-ai/web": "^5.9.0",
    "chalk": "^5.3.0",
    "cli-table3": "^0.6.3",
    "commander": "^12.0.0",
    "js-yaml": "^4.1.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  }
}
