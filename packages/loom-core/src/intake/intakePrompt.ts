/**
 * Intake sizing instruction — owned by story-023-002.
 * This stub satisfies the import seam so story-023-001 can compile and test
 * independently. Story-023-002 replaces the body with the full conservative
 * epic-vs-story criteria (ADR-006).
 */
export function buildIntakeSizingInstruction(): string {
  return '';
}
