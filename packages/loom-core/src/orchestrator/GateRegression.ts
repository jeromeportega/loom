import type { RegressionFinding } from './FinalizeGates.js';

/**
 * Stub — story-077-004 replaces this file entirely with the real implementation.
 * When priorContracts is empty, returns [].
 */
export function checkCrossEpicRegressions(_opts: {
  epicDiff: string;
  storyDiffs: Map<string, string>;
  priorContracts: Map<string, string[]>;
}): RegressionFinding[] {
  return [];
}
